/**
 * Factorial
 * @param {number} n
 * @returns {number}
 */
function factorial(n) {

  //Для удобства счета еще одна переменная К;

  let k = 1;

  //Учесть факториал 0=1  и 1=1;

  if (n === 0 || n === 1) {

   return k;

  }

  //Считаем факториал и еще одна переменная

  for (let = i = 1 ; n >= k ; i = i * k++ ) {

  }
  return i ;
}

factorial(10);